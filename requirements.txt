pandas==2.2.3
numpy==2.2.1
pydantic==2.10.4
pydantic-settings==2.7.1
PyYAML==6.0.2
python-dotenv==1.0.1
requests==2.32.3
tabulate==0.9.0
pytest==8.3.4
pytest-cov==6.0.0

# mt5linux's published metadata pins an unrelated, ancient dev-environment
# freeze (ancient cffi/cryptography/twine/keyring/SecretStorage) that it does
# not actually need at runtime -- the client class only imports rpyc + numpy.
# Install separately with --no-deps (after pip install -r requirements.txt):
#   pip install --no-deps mt5linux==0.1.9 rpyc==5.0.1
# Use rpyc 5.0.1 — must match the Wine-side RPyC server (5.x wire protocol).
